const listInfo = document.getElementById('list-info');
const addNewInfo = document.getElementById('add-new-info');
const contactInfo = document.getElementById('contact-info');

export { listInfo, addNewInfo, contactInfo };
